import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hello_world_final',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
